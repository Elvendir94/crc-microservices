spring:
  cloud:
    stream:
      function:
        definition: processDomainObject
      bindings:
        processDomainObject-in-0:
          destination: crc_topic_domain_object
          group: crc-service-c
          binder: kafka-crc
      default-binder: kafka-crc
