spring:
  cloud:
    stream:
      bindings:
        domainObjectProcessor-in-0:
          destination: domain-object-topic
          group: crc-service-c
          binder: kafka-crc
      default-binder: kafka-crc
    function:
      definition: domainObjectProcessor